// SwiftUI entry point for AirGapSync

import SwiftUI

@main
struct AirGapSyncApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct ContentView: View {
    var body: some View {
        Text("AirGapSync UI Placeholder")
            .padding()
    }
}
